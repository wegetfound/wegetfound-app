# Vite exposes only VITE_-prefixed vars to the browser. The anon/publishable key
# is safe to ship to the client; the service-role key must NEVER appear here.
VITE_SUPABASE_URL=https://[project-ref].supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...
VITE_API_URL=http://localhost:3001
